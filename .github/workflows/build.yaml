name: Build

on:
  push:
    branches: [ 'develop', 'feature/**', '*fix/**', 'release/**' ]
  pull_request:
    branches: [ 'main' ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-latest, windows-latest]
        config: [Debug, Release]
        include:
          - os: ubuntu-24.04
            cc: clang-18
            cxx: clang++-18
            generator: Ninja
            shared-libs: true
            testing: true
          - os: macos-latest
            cc: gcc-14
            cxx: g++-14
            generator: Ninja
            shared-libs: true
            testing: true
          - os: windows-latest
            config: Debug
            shared-libs: false
            testing: true
          - os: windows-latest
            config: Release
            shared-libs: true
            testing: false

    runs-on: ${{matrix.os}}

    steps:
    - uses: actions/checkout@v4

    - name: Install generator Ninja
      if: ${{matrix.generator == 'Ninja'}}
      uses: seanmiddleditch/gha-setup-ninja@v5

    - name: Generate build system (Ubuntu/macOS)
      if: ${{matrix.generator == 'Ninja'}}
      run: cmake -B ${{github.workspace}}/build
                 -G ${{matrix.generator}}
                 -DCMAKE_C_COMPILER:STRING=${{matrix.cc}}
                 -DCMAKE_CXX_COMPILER:STRING=${{matrix.cxx}}
                 -DCMAKE_BUILD_TYPE:STRING=${{matrix.config}}
                 -DBUILD_SHARED_LIBS:BOOL=${{matrix.shared-libs}}
                 -DBUILD_TESTING:BOOL=${{matrix.testing}}

    - name: Generate build system (Windows)
      if: ${{matrix.os == 'windows-latest'}}
      run: cmake -B ${{github.workspace}}/build
                 -DBUILD_SHARED_LIBS:BOOL=${{matrix.shared-libs}}
                 -DBUILD_TESTING:BOOL=${{matrix.testing}}

    - name: Build targets
      run: cmake --build ${{github.workspace}}/build
                 --config ${{matrix.config}}

    - name: Perform tests
      if: ${{matrix.testing}}
      working-directory: ${{github.workspace}}/build
      run: ctest -C ${{matrix.config}} -V

    - name: Create package
      if: ${{false}}
      working-directory: ${{github.workspace}}/build
      run: cpack -C ${{matrix.config}} -G ZIP -V
